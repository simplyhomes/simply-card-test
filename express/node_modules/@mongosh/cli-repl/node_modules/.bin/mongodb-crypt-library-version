#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../../mongodb-crypt-library-version/bin/mongodb-crypt-library-version.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../../mongodb-crypt-library-version/bin/mongodb-crypt-library-version.js" "$@"
  ret=$?
fi
exit $ret
